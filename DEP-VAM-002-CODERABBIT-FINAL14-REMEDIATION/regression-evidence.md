# Regression Evidence

## RED

The exact three-node selection returned `3 failed`. The failures independently
proved canonical-policy fail-open, SQLite page fail-open, and missing PATCH /
DELETE OpenAPI 400 responses. The bounded derivative is
`shareable/artifacts/terminal--final14-red.txt`.

## Green

At implementation commit `9308751b8616e5cccd51f2f1d0c00ca35f01b558`,
the repository-pinned focused selection covering the access policy, complete
change-envelope module, HTTP/OpenAPI mapper, authoritative rollback, all older
disposition regressions, and this 14-item regression returned `30 passed`.
Changed-Python Ruff, module-size, `git diff --check`, governance Doctor, and CI
contract verification passed. The bounded derivative is
`shareable/artifacts/terminal--final14-green.txt`.

The real loopback PATCH/DELETE assertions are committed in
`test_gateway_http_memory_api_facade_routes` but are not claimed as locally
executed in this sandboxed focused result. They remain part of the required
repository-controlled Local Green proof.
