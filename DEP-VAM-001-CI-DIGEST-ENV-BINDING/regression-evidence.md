# Regression Evidence

## Regression test added or strengthened

Hosted governance verification is the executable regression gate. The DEP also
records both pre-fix calculations against the same exact base/head.

## Related tests executed

Default digest `e155240b...`; CI-contract digest `1ba66ab1...`; hosted job
`96813503486` failed with `merge gate change_digest does not match`.

Complete Local Green for the remediation tree passed all 446 isolated Subject
nodes and 2,928 repository tests with 10 skips.

## Unaffected paths sampled

Product runtime, frozen Subject artifacts, tests, documentation decisions,
database, API, and Issue state are unaffected by the audit calculation context.
