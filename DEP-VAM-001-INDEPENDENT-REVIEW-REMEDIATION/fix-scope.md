# Fix Scope

## Smallest sufficient change

- Add one Markdown section extractor and bind all four issue outcomes to it.
- Assert complete forbidden dependency statements and DLI runtime ownership.
- Rewrite the authoritative rollback to target only compatibility wording,
  require a fresh consumed L3 approval plus strict DEP, and preserve provenance.
- Align the ADR rollback paragraph with the authoritative procedure.
- Refresh exact merge-gate metadata after the non-audit fix commits.

## Non-scope

No runtime, frozen Subject artifact, progress ledger, GitHub Issue state,
Memory API, database, release, or Digital Life Identity implementation change.

## Blast radius

Documentation contract tests, VAM-001 rollback/governance evidence, and the
audit-only merge-gate metadata.
