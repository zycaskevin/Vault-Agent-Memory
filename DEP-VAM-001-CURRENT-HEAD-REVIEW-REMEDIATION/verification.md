# Verification

## Green command and result

The focused boundary, rollback, Subject contract, and exact baseline suite
passed 58 tests. The canonical progress validator, Ruff, `git diff --check`, and
strict verification of the supplemented local-target DEP passed. Complete Local
Green then passed all 446 identity-isolated Subject nodes and 2,928 repository
tests with 10 skips. Hosted CI and fresh exact independent review remain required
after the gate is rebound.

## Before/after evidence

Before: merge-wide ancestry and incomplete evidence selectors. After: exclusive
head-parent provenance, exact checkout facts, warning disposition, corrected
mutation semantics, and immutable reverse-order rollback identities.

## Remaining limitations

No rollback or merge is authorized. Independent Reviewer Local Green still
requires an allowed clean execution environment. The one repository warning is
the already-dispositioned invalid escape sequence and remains visible.
