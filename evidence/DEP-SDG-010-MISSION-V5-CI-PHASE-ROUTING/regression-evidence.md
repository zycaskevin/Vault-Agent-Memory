# Regression Evidence

## Required checks

- Candidate exact linear topic PASS with inactive replay.
- Candidate exact active delivery plus governed descendant PASS with delivery
  replay; active single-parent topic DENY.
- Purported invalid delivery must DENY without candidate fallback. Extra path,
  pending artifact, proof mismatch, and invalid
  or abbreviated phase DENY.
- Identity collection remains closed and every Mission V5 node runs once under
  the explicit phase harness.
- Updater and task-action paths never receive a candidate capability.

## Executed results

- Focused candidate-routing suite: 92 passed in 46.37 seconds.
- One Builder Local Green: candidate identity isolation passed 431 nodes;
  disjoint remainder passed 2869 tests with 12 skipped and one pre-existing
  SyntaxWarning in 94.73 seconds.
- No unrelated same-repository test process was present during either run;
  raw logs and exit markers remain under the externally coordinated lease.
- Strict DEP verification is recorded separately before the frozen handoff.
- Rollback resolves exactly one merged SDG-010 source branch PR, requires two
  parents, and verifies canonical inactive dispatcher/progress invariants
  without invoking a reverted phase harness.
