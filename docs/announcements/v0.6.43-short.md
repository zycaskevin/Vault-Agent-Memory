# Vault-for-LLM v0.6.43

This is a small setup-agent UX fix release.

## Fixed

- `vault setup-agent` no longer asks to install optional Python dependencies when the user only selects the normal core+MCP path.
- Added a regression test for the core+MCP interactive install flow.

No database or schema behavior changed in this release.
